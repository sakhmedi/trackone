# OCR
# Указаны минимальные версии (>=), а не точные (==): так pip подберёт сборку,
# совместимую с версией Python судьи, и установка не сломается на новом Python.
pytesseract>=0.3.10
pdf2image>=1.16.0
Pillow>=10.0.0

# (для Track 3, понадобится позже)
# pyproj>=3.5.0
